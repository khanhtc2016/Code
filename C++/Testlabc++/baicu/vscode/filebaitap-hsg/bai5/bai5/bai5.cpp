//đề bài: nhập k nếu k !=0 thì in ra "k ko phải số 0"
#include "bits/stdc++.h"

int main()
{
	(void) _setmode(_fileno(stdout), _O_U8TEXT);
	int k;
	cin >> k;
	if (k!=0)
	{
		wcout << L"k không phải số 0";
	}

}
